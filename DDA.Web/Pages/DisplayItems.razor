@using DDA.ApiModels;


@*@if (Items != null)
{
    var itemsPerRow = 1;
    var itemRowList = new List<ItemModel>();
    @for (int i = 0; i < Items.Count(); i++)
    {
        itemRowList = Items
        .Skip(i * itemsPerRow)
        .Take(itemsPerRow)
        .ToList();
*@
<div class="row">
        @foreach (var item in Items)
        {
            <div class="col-md-3 mb-2 item">
                <a href="/ItemDetails/@item.Id">
                    <div class="card item-card">
                        <img class="img-thumbnail item-img" src="@item.Image">
                        <div class="card-body">

                            <h5 class="card-title mb-3">
                                @item.Name
                            </h5>
                            <p class="card-text">
                                <b>@item.Price.ToString("C")</b>
                            </p>
                        </div>
                    </div>
                </a>
            </div>
        }
</div>
    
@*}*@


        
@code {
    [Parameter]
    public IEnumerable<ItemModel> Items { get; set; }
}